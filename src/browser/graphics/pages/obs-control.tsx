import React from 'react';
import { render } from '../../render';
import { ObsSceneControl } from '../features/survivals/obs-scene-control';

const App = () => {
  return <ObsSceneControl />;
};

render(<App />);
